/**
 * @description
 * Default values used throughout the application.
 */

/**
 * Default aria-label text for animated content.
 */
export const DEFAULT_ARIA_LABEL = 'animated content';

/**
 * Default fallback text when text extraction fails.
 */
export const FALLBACK_TEXT = '';
